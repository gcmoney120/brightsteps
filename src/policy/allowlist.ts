/**
 * Agent OS Policy Allowlist
 * Defines the authorized actor keys for the Agent OS governance system.
 */
export const POLICY_ALLOWLIST: Record<string, boolean> = {
  command: true,
  atlas: true,
  forge: true,
  sentinel: true,
  compass: true,
};
